<!--
Session trace generated by `coding-scaffold session init`. Edit this file as you work — or have
the agent edit it. The structured fields below are read by `coding-scaffold session summarize`.

Sections beginning with `##` are parsed by name; bullet-list items under each section are
counted. Free-form prose between sections is preserved.
-->

# Session Trace — 2026-09-19

## Task

Refresh MCP and skill governance; add offline compatibility checks and optional lifecycle hooks

## Plan

<!-- 2-5 bullets: what the agent intends to do, in order. -->

- Detect Claude project MCP configuration and fail readiness on unreadable configurations.
- Unify project skill discovery and version full-package approvals with explicit legacy migration.
- Add offline native configuration contracts, provenance and doctor freshness findings.
- Generate optional Codex/Claude lifecycle hooks; test preservation, privacy and loop prevention.
- Document adoption and verify the full suite and repository smoke checks.
- Prepare v0.8.0, validate the package and docs, and publish after CI passes.

## Files Inspected

<!-- Bulleted list of files the agent read but did not modify. -->

- AGENTS.md, pyproject.toml, existing adapters, updater, doctor, MCP and skill implementations.
- Existing CLI help, golden outputs, upgrade tests and scaffold fixture factories.
- Official Agent Skills, Codex configuration/hooks, Claude settings/MCP/hooks and OpenCode documentation.

## Files Changed

<!-- Bulleted list of files the agent wrote to. Include intent in one line each. -->

- src/coding_scaffold/mcp.py and eval_harness.py: detect .mcp.json and reject malformed candidate configuration.
- src/coding_scaffold/skills.py and skill_metadata.py: native locations, required scalar frontmatter, complete-package checksums and migration handling.
- src/coding_scaffold/compatibility.py: offline field checks, source/review metadata and freshness warnings.
- src/coding_scaffold/hooks.py: optional native configuration merging and bounded local lifecycle handler.
- src/coding_scaffold/adapters.py, doctor.py, cli.py and cli_help.py: expose and integrate the new behavior.
- src/coding_scaffold/templates/writers/scaffold.gitignore: ignore local aggregate hook reports.
- tests/test_native_skills.py, test_compatibility.py, test_hooks.py and fixtures: regression, contract, migration and offline execution coverage.
- CHANGELOG.md and wiki Security, Skills and Agents, Tool Adapters, Upgrading: describe usage and limitations.
- .gitignore: include this review trace and exclude generated local graphify output.
- pyproject.toml, uv.lock and CHANGELOG.md: promote the accumulated changes to v0.8.0.

## Commands Run

<!-- Exact commands, one per bullet. Include exit codes if non-zero. -->

- coding-scaffold session init --target . --task "Refresh MCP and skill governance; add offline compatibility checks and optional lifecycle hooks"
- UV_CACHE_DIR=/private/tmp/coding-scaffold-uv-cache uv run --offline ruff check
- UV_CACHE_DIR=/private/tmp/coding-scaffold-uv-cache uv run --offline pytest -q
- UV_CACHE_DIR=/private/tmp/coding-scaffold-uv-cache uv run --offline coding-scaffold context lint --target .
- UV_CACHE_DIR=/private/tmp/coding-scaffold-uv-cache uv run --offline coding-scaffold eval run --target .
- UV_CACHE_DIR=/private/tmp/coding-scaffold-uv-cache uv run --offline coding-scaffold tools compatibility --target .
- git diff --check
- Intermediate targeted runs caught expected golden-path additions, an unused test import and a fixture using manual mode; each was corrected.
- Release validation: `uv lock`, `uv sync --locked --extra dev`, `uv run --offline --locked ruff check`, `uv run --offline --locked pytest -q` and the context/eval/compatibility smoke commands above.
- `uv build --offline --out-dir /private/tmp/coding-scaffold-v0.8.0-dist` and `npm run build` from docs/.

## Test Result

<!-- Summarize. Examples:
- Passed: 167
- Failed: 0
- Skipped: 1
-->

- Passed: 734 tests (66 added); failed: 0; skipped: 0. Release validation completed in 2.98 seconds.
- Ruff and context lint passed; readiness passed 9/9 enabled checks.
- Compatibility: zero errors; one missing-provenance warning in this repository, which has not adopted a generated compatibility manifest.
- Wheel and source distribution built successfully. Documentation built successfully with a non-fatal local Rspack cache-write warning.
- Installed the built wheel into an isolated Python 3.14 environment; verified version 0.8.0, new module imports, native hook generation, native skill creation and compatibility checks.

## Risks

<!-- Anything the change touches that a reviewer should look at carefully. -->

- Required frontmatter is checked with a documented scalar subset, not a general YAML parser.
- Checks cover reviewed native fields and dated documentation snapshots; installed tool versions and live model availability are not probed.
- Lifecycle hooks check scaffold metadata, not project test results or tool permissions. Native hook execution/trust was not exercised in a live agent session.
- Whole-package checksums are unsigned local drift markers; legacy approvals require review and reapproval.
- No new dependencies, runtime network/model calls or telemetry. Local hook reports contain counts and a timestamp, not transcripts.

## Follow-up Recommendations

<!-- Things deliberately not done in this session. -->

- Review native hooks before enabling them in a target project; keep the CLI on the agent's PATH.
- Review upstream contracts and update fixtures and provenance together when the review window expires.
- Jev remains outside this implementation scope.

## Reusable Knowledge Discovered

<!-- Knowledge nudge: at handoff, ask the active coding agent to use its configured model to spot
durable lessons worth remembering. Capture only reviewable candidates here: project facts, team
preferences, decisions, failed attempts, useful commands, gotchas, or reusable prompts. Do not paste
raw chat transcripts, secrets, personal data, or transient conversation. Once promoted, link the
promoted file from the relevant bullet. -->

- Claude project MCP definitions use root .mcp.json with mcpServers, not settings.json's legacy mcp mapping.
- Native Agent Skills do not require a scaffold manifest. Project-local aliases must be deduplicated without reading external skill roots.
- Approving only SKILL.md and manifest.json misses executable helper changes; hash complete package paths, contents and executable bits.
- Hooks need an explicit stop-loop guard and must not confuse metadata readiness with successful project tests.
