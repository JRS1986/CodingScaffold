<!--
Session trace generated by `coding-scaffold session init`. Edit this file as you work — or have
the agent edit it. The structured fields below are read by `coding-scaffold session summarize`.

Sections beginning with `##` are parsed by name; bullet-list items under each section are
counted. Free-form prose between sections is preserved.
-->

# Session Trace — 2026-05-25

## Task

Add agent-side knowledge nudge guidance

## Plan

<!-- 2-5 bullets: what the agent intends to do, in order. -->

- Inspect current session, adapter, knowledge, memory, and docs surfaces.
- Add agent-side knowledge nudge guidance without adding CodingScaffold runtime LLM calls.
- Generate native `knowledge-propose` command/skill artifacts for supported coding environments.
- Add tests, update golden fixtures, and run required verification.

## Files Inspected

<!-- Bulleted list of files the agent read but did not modify. -->

- `src/coding_scaffold/memory.py` for existing reviewable memory semantics.
- Existing generated adapter templates under `src/coding_scaffold/templates/`.
- Existing adapter, session, docs-positioning, and golden-output tests.

## Files Changed

<!-- Bulleted list of files the agent wrote to. Include intent in one line each. -->

- `src/coding_scaffold/adapters.py` - emits native `knowledge-propose` artifacts for OpenCode, Claude Code, and Codex.
- `src/coding_scaffold/templates/adapters/knowledge-propose.md` - shared agent-side prompt for reviewable knowledge proposals.
- Adapter and writer templates - add persistent Knowledge Nudge guidance and first-session/agentic-change reminders.
- `src/coding_scaffold/session.py` - strengthens `Reusable Knowledge Discovered` guidance in new session traces.
- `README.md`, `docs/docs/wiki/FAQ.md`, `docs/docs/wiki/Knowledge-Base.md` - document the no-runtime-LLM boundary and nudge behavior.
- Tests and golden fixtures - assert new generated files and docs positioning.

## Commands Run

<!-- Exact commands, one per bullet. Include exit codes if non-zero. -->

- `uv run coding-scaffold session init --target . --task "Add agent-side knowledge nudge guidance"`
- `uv run pytest -q tests/test_adapters.py tests/test_session_and_pr_template.py tests/test_docs_positioning.py`
- `uv run ruff check src/coding_scaffold/adapters.py src/coding_scaffold/session.py tests/test_adapters.py tests/test_session_and_pr_template.py tests/test_docs_positioning.py`
- `uv run ruff check`
- `uv run pytest -q` (first run: 2 golden fixture failures; fixed fixtures)
- `uv run pytest -q`
- `uv run coding-scaffold context lint --target .`
- `uv run coding-scaffold eval run --target .`
- `uv build`

## Test Result

<!-- Summarize. Examples:
- Passed: 167
- Failed: 0
- Skipped: 1
-->

- Passed: 333 pytest tests; ruff; context lint; eval 9/9; uv build.
- Failed: 0 after golden fixture updates.
- Skipped: context lint reported 10 missing optional context file candidates as skipped, with 0 findings.

## Risks

<!-- Anything the change touches that a reviewer should look at carefully. -->

- Native command/skill wording must clearly avoid raw transcripts, secrets, personal data, and silent curated-wiki writes.
- Adapter golden outputs now include one extra generated command/skill per supported native environment.
- This is agent-side prompting only; CodingScaffold still does not call models at runtime.

## Follow-up Recommendations

<!-- Things deliberately not done in this session. -->

- Consider a future deterministic `knowledge harvest --source sessions` command that only converts already-marked session bullets into reviewable `.new` files.
- Consider adding adapter-specific examples once OpenCode, Claude Code, and Codex command conventions evolve.

## Reusable Knowledge Discovered

<!-- If a skill, decision, or wiki page should be promoted from this session, name it here.
Once promoted, link the promoted file from this bullet. -->

- The safest way to make knowledge capture feel automatic while preserving deterministic scaffold behavior is to nudge the active coding tool's configured model, then require reviewable session bullets or `.new` proposal files before promotion.
